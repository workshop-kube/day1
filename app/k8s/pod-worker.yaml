apiVersion: v1
kind: Pod
metadata:
  name: demo-ruby-worker
  labels:
    app: demo-ruby-worker
spec:
  containers:
    - name: worker-container
      image: ${IMAGE}
      command: ["ruby"]
      args: ["worker.rb"]